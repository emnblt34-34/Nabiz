# Nabız sunucusu için gerekli paketler. basla-mac.command / basla-windows.bat
# bunları otomatik kurar.
fastapi>=0.110
uvicorn>=0.29
requests>=2.31
feedparser>=6.0

# Öngörü (forecast) katmanı için gerçek fiyat verisi (ücretsiz).
yfinance>=0.2

# OPSİYONEL — öngörünün ML üst katmanı (lojistik regresyon). Kurmazsan şeffaf
# kural modeli tek başına çalışır.
scikit-learn>=1.3

# OPSİYONEL — lokal FinBERT (daha nüanslı sentiment). Kurmazsan sözlük kullanılır.
# transformers>=4.40
# torch>=2.2
